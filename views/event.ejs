<%- include('header') -%>

<h1><%= title %></h1>
<div><%=info %></div>
<% if (regs.length === 0) { %>
    <p>Enginn skráður</p>
<% } else { %>
        <% regs.forEach((row) => { %>
            <div><%= row.name %></div>
            
            <div><%= row.comment %></div>
        <% }) %>
<% } %>
<a href="/">Til baka</a></p>
<%- include('footer') -%>